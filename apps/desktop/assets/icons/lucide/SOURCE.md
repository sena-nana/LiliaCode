图形路径来自历史 Git `2eec58e` 的 yarn.lock 锁定的 `@lucide/vue` 1.30.0，包内 `dist/esm/icons/plus.mjs` 与 `save.mjs`。

来源：https://registry.npmjs.org/@lucide/vue/-/vue-1.30.0.tgz

保留默认 24×24 viewBox、2px stroke、round linecap/linejoin；使用 currentColor 跟随 Native 按钮主题。许可证见 LICENSE。
